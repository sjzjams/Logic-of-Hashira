// Replace this file by running `flutterfire configure` in the project root.
// See lib/firebase_options.example.dart and README.md (Firebase setup).

import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

/// Placeholder until FlutterFire generates platform-specific options.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase is not configured. Run `flutterfire configure` from the '
      'project root, then rebuild. See README.md — Firebase setup.',
    );
  }
}
